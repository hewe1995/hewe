package com.hewe.web.controller;

import org.springframework.stereotype.Controller;

@Controller
public class LoginController {
	/*
	 * 
	 * @Autowired private UserDao userDao;
	 * 
	 * @RequestMapping(path = "/login", method = RequestMethod.GET) public
	 * String login() { return "login"; }
	 * 
	 * @RequestMapping(path = "/save", method = RequestMethod.GET) public String
	 * save() { UserModel user = new UserModel(); user.setId("2323");
	 * user.setPassword("dddd"); user.setStatus(1); user.setUsername("user");
	 * userDao.add(user); return "success"; }
	 * 
	 * public UserDao getUserDao() { return userDao; }
	 * 
	 * public void setUserDao(UserDao userDao) { this.userDao = userDao; }
	 * 
	 */}
